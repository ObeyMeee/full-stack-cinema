import { HasLowerCaseDirective } from './has-lower-case.directive';

describe('HasLowerCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new HasLowerCaseDirective();
    expect(directive).toBeTruthy();
  });
});
