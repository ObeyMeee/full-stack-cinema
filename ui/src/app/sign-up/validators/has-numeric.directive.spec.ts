import {HasNumericDirective} from './has-numeric.directive';

describe('HasNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new HasNumericDirective();
    expect(directive).toBeTruthy();
  });
});
