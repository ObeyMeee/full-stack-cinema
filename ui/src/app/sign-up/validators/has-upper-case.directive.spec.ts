import { HasUpperCaseDirective } from './has-upper-case.directive';

describe('HasUpperCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new HasUpperCaseDirective();
    expect(directive).toBeTruthy();
  });
});
